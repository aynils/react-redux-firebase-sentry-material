export const app = {
  name: 'MyApp',
};

export const firebase = {
  // insert your firebase config here
};

export const reduxFirebase = {
  userProfile: 'users',
  useFirestoreForProfile: true,
  enableLogging: false,
};

export const sentryUrl = '' // insert your Sentry Url here
